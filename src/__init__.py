"""
src — Project Lumi Python backend root package.

Sub-packages:
    audio/  — microphone capture, wake word, STT
    core/   — configuration, events, state machine, orchestrator, logging, startup checks
    llm/    — local LLM integration (planned — Phase 3 remaining)
"""
